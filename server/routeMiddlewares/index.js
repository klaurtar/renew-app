module.exports = {
    CustomMulter: require('./CustomMulter').CustomMulter,
    Authentication: require('./Authentication').Authentication,
    EndWith:  require('./EndWith').EndWith,
    ModifyHeader: require('./ModifyHeader').ModifyHeader,
}
